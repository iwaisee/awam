Finish verification of the already-implemented drawer changes (no new code expected unless a check reveals a bug)

Current state
- All code from the four refinement specs is implemented in src/components/feed/WorkOrderDrawer.tsx + src/lib/trackDossiers.ts, passing tsc and eslint.
- Visually confirmed already: amber active theme (#SKT-1042), unified metadata bar, citizen narrative card, stacked supervisor box, ghost call button, sticky "Open Live Tracker" CTA.
- Interrupted mid-check: the green "Resolved & Verified" theme on the new #SKT-0856 dossier.

Remaining steps (short browser checks, ~1 minute each, against the running dev server)
1. Open /feed, click the resolved card (#SKT-0856 "Broken asphalt crater…") → confirm the emerald assignment card: ShieldCheck icon, "Resolved & Verified" badge, "Fixed in 47h" pill, emerald ghost call button, all-completed timeline.
2. Regression: press Escape → drawer slides out, body scroll + padding restored, panel parks off-screen.
3. Optional quick 375px viewport pass to confirm the new metadata bar and stacked supervisor box wrap without horizontal overflow.

If any check exposes a visual bug, fix it in the drawer component and re-verify. Otherwise report results and stop — no further code changes planned.