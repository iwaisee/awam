"use client";

/* /admin/territories/cities — Tier 2 · District & City (ضلع / شہر) manager. */

import { TerritoryLevelPage } from "../territoryLevelPage";

export default function CitiesPage() {
  return <TerritoryLevelPage level="cities" />;
}
