"use client";

/* /admin/overview — Command Radar (same console landing as /admin). */

import OverviewView from "../views/OverviewView";

export default function OverviewPage() {
  return <OverviewView />;
}
