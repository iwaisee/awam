"use client";

/* /admin/territories/zones — Tier 3 · Tehsil & Zone (تحصیل / زون) manager. */

import { TerritoryLevelPage } from "../territoryLevelPage";

export default function ZonesPage() {
  return <TerritoryLevelPage level="zones" />;
}
