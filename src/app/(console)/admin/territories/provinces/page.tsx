"use client";

/* /admin/territories/provinces — Tier 1 · Province (صوبہ) manager. */

import { TerritoryLevelPage } from "../territoryLevelPage";

export default function ProvincesPage() {
  return <TerritoryLevelPage level="provinces" />;
}
